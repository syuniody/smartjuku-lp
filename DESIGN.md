# DESIGN.md — スマ塾サイトのデザイン規約

このファイルが唯一の正解。**ここに無い値をその場で発明しない。**
AI（Claude / Codex 等）に制作・改修を任せるときは、必ず最初にこれを読ませる。

作業ディレクトリ：`~/Desktop/smartjuku-lp-deploy`
**適用範囲は次の2つ。片方だけ直さない。**
- `index.html`（LPは自己完結の inline `<style>`）
- `assets/site.css`（記事44本・一覧ページ）
- `tokushoho.html` / `privacy.html`（各ページが自己完結の inline `<style>`。
  **器は tokushoho.html を正とし、privacy.html はそれを複製して中身だけ差し替える**）

**適用範囲外：`demo/edubiz-tsushin/`。**
すららネットの媒体「Edu-biz通信」で、スマ塾のページではない。
別規約 `demo/edubiz-tsushin/DESIGN.md` に従う。互いに値を持ち込まないこと。

2026-08-18 の時点で `assets/site.css` は規約が未適用のままだった（角丸の直値10種、
`999px` と `100px` の併存、影が規定値と 0.01 違う近似値）。同じことを繰り返さないため、
**改修後は必ず両方のファイルでレンダリング実測すること。**

---

## Signature — 記憶に残す一点

**赤ペンの丸つけ。** ファーストビューの「月額0円」を、先生が正解に丸をつけるように囲む。

なぜこれか：塾の世界に実在する道具（採点の赤ペン）であり、同時にスマ塾の商品の核心
（競合はサブスク、うちは月額0円）と、主力サービス（無料HP診断＝他塾のHPに赤を入れる）の
両方を一つの動作で言い切れるから。SaaS一般にも、AIスタートアップにも流用できない。

**規約：**
- 赤ペン（`--akapen`）は、このファーストビューの丸つけと、フォームのエラー表示にだけ使う
- 装飾として赤を足さない。丸つけを2つ以上のセクションに置かない
- **大胆さはここだけ。** 他は静かに保つ（余白・罫線・明朝・ネイビー）
- 実装：`.maru` + インラインSVG。`prefers-reduced-motion` と `html.noanim` で描画アニメを止める

---

## 色

```
--paper    #F7F4ED   紙。LPでは「面」だけに使う（地色ではない）。記事では地色
--white    #FFFFFF   LPの地色。面の上に乗るカード・フォーム
--ink      #22252B   本文
--ink-soft #555962   補助文。注記・キャプション・入力ヒントだけに使う
--blue     #24519E   主色。学校・信頼。CTAと見出しラベル
--blue-deep#1B3D78   hover・強調。ファーストビュー右の「担当者の面」の地
--akapen   #D6402C   Signature（丸つけ）とエラー。それ以外に使わない
--marker   #FFE9A0   蛍光ペン。本文中の限定強調と選択範囲のみ。見出しには使わない
--line     rgba(34,37,43,.22)  罫線。白地では .16 が消えるため .22
```

**`--paper` は地色ではなく「面」の色（2026-09-04 に変更）。**
LPの地色は白 `#FFFFFF`。クリーム `#F7F4ED` を敷いてよいのは次の「面」だけで、
ページ全体には敷かない。

- セクションの帯（セルフチェック・サービス・制作例・料金・運営者）
- 引用・FAQの折りたたみ（`details`）
- 料金カード・入力欄・ラジオ

**面の上に乗るカード（`.work` / `.plan` / `.plan-band`）は白に戻す。**
クリームの面にクリームのカードを置くと、カードの輪郭が消える。

**クリーム地をやめた理由（2026-09-04）**

2026-08-18 の時点では、クリーム地＋明朝を「AIの既定値ではなく選び直した配色」として
サイト全体で維持していた。この判断を、2026-09-04 の design-gate と
**学習塾HP制作の他社12サイトとの実測比較**で見直した。

- **暖色の地を使っているのはスマ塾だけ**（13社中1社）。差別化として効いておらず、
  むしろ「AIが未指定のとき最も出やすい配色」という指摘のほうが当たっていた
- **本文のコントラスト比 7.0 で13社中11位。** クリーム地＋`--ink-soft` の本文が主因
- **ファーストビューの文字量が2位。** 読ませる前に情報で埋めていた

**結論：LPの地色は白 `#FFFFFF` に変更する。クリーム `#F7F4ED` は「面」にだけ使う。**
クリームを捨てたのではなく、**全面に敷くのをやめて、面の色に格下げした。**
赤ペンの丸つけ（Signature）は変更なし。価格チップ「月額保守費0円」の1か所だけで、
白地になっても丸つけが一点であることは変わらない。

**ファーストビューの構図（2026-09-04 から標準）**

**左＝白地に見出しと赤ペン、右＝紺 `--blue-deep` の面に担当者**、の2分割を標準とする。

- 紺の面には**出席簿の横罫**を敷いてよい（白 12〜16% 透過・1px・間隔 28〜32px）。
  画像は使わず CSS のグラデーションで引く。綴じ側の縦罫1本まで足してよい
- 面には**担当者の根拠**（Edu-NEWSの登録者数と視聴時間・診断塾数・経歴）と
  **実写の切り抜き**を組む。写真だけを置かない
- 赤ペンは左の価格チップ「月額保守費0円」の丸1か所のみ（変更なし）

**本文の型**

- 本文は **16px・`--ink`**。`--ink-soft` は注記・キャプション・入力ヒントだけに残す
- `--line` は白地用に **.22**（クリーム地の .16 では白の上で消える）
- 明朝は h1・h2 と主役の数字だけ。カードの h3 はゴシック700に落とす

**記事ページ（`assets/site.css`）は今回は据え置き、クリーム地のままとする。**

LPと記事で地色が違うことになるが、これは分断ではなく、2026-08-18 の検討結果を
そのまま生かしている。記事を純白にする案はこの日に実物を3案並べて比較して却下した：

- **純白 #FFFFFF**……本文コントラストは最高（15.36:1）だが、**結論ブロック・データ表・
  調査概要の白いカードが地と同化する**（色差0）。地色だけの差し替えでは済まず、
  カード設計のやり直しになる
- **わら半紙寄り #F4F1E8**……**並べないと分からない程度の差**
- **現行 #F7F4ED を維持**（採用）

LPは白いカードをほとんど持たず、面とカードの関係を組み替えるだけで白地に移せた。
記事44本は白いカードが本文構造そのものなので、同じ手が使えない。
**記事の地色を白にするときは、カード設計から引き直すこと。地色だけ差し替えない。**

赤ペンは「主張を採点する」箇所だけに使い、**記事テンプレートでは使わない**（変更なし）。
記事44本に赤ペンを置くと、44回繰り返された時点でSignatureではなく
ブランドスタンプになり、かえってAI生成感が強まる。

### カテゴリ色（お役立ち情報の分類のみ）
```
#24519E 塾HPの基礎知識 ／ #B0553A 教育ニュース ／ #7A5C2E 調査レポート ／ #17706A 制作事例
```
分類の識別用。装飾に転用しない。

---

## 書体

```
--serif "Shippori Mincho"      見出し（h1 / .sec-title / 数字の主役）
--sans  "Zen Kaku Gothic New"  本文・UI・ラベル
```
**この2つ以外を増やさない。** 欧文専用フォント（Inter等）を本文に足さない。
（2026-09-05まで署名用に Klee One があったが、代表コメント節の廃止とともに削除。
書き文字の書体を戻したくなっても、登場箇所が1つに限られない限り足さない。）

---

## 文字サイズ（8段階）

2026-09-04 の時点で `index.html` の inline `<style>` は font-size を51種類（うち固定remが33種類）
使っていた。`.82rem` `.86rem` `.88rem` のように**並べても区別のつかない値が乱立**していた
だけで、意味の差ではない。**この8段階以外を使わない。**

```
--fs-1   .68rem   10.88px   バッジ・注記の最小（.req / 小ラベル）
--fs-2   .74rem   11.84px   補助テキスト・キャプション
--fs-3   .80rem   12.80px   注記・フォームのヒント・表の補足
--fs-4   .86rem   13.76px   UI・ラベル・ナビ・カード本文
--fs-5   .95rem   15.20px   本文
--fs-6  1.02rem   16.32px   リード・ボタン・強調した本文
--fs-7  1.30rem   20.80px   小見出し（h3・カード見出し・ロゴ）
--fs-8  1.70rem   27.20px   セクション内の主役（数字・運営者名）
```

**段階に入れないもの（現状値を優先する）：**

- **`clamp()` の流体見出し**（`.hero h1` / `.sec-title` / `.svc-price .n` など）。
  幅で伸縮させるのが目的で、固定段階に丸めると1440と390のどちらかが壊れる
- **`em` 指定**（`.hero h1 .h1-price` の `.72em` など）。親サイズへの相対比率であり、
  親を変えれば追随する。絶対値に置き換えない
- **`.logo small` の `.6rem`**。追従ヘッダーの高さはDESIGN.mdで内訳まで固定してあり
  （CTA 48px＋ナビ帯 42px＋余白21px）、ここを上げると390pxで帯が伸びる
- **`.plan-band-price .n` の `2.1rem`**。料金の数字は主役なので現状値のまま

新しい大きさが要るときは、**段階を増やさずに近い段階へ寄せる**。
どうしても足りない場合だけ、8段階そのものを引き直す。

---

## 角丸（この4つ以外を使わない）

```
--r-xs   4px    タグ・小さな囲み
--r-sm   8px    入力欄・画像・ボタン
--r-md  14px    カード・テーブル・フォーム
--r-pill 999px  ピル型ラベル
```
円形（アバター・ドット）だけ `50%` を使ってよい。
`10px` `16px` `18px` のような「近い値」をその場で作らない。

## 影（この3つ＋フォーカスリング以外を使わない）

```
--sh-line   0 1px 0 var(--line)                 ヘッダーの境界など
--sh-1      0 14px 34px rgba(34,37,43,.13)      カードの浮き（小）
--sh-2      0 20px 48px rgba(34,37,43,.16)      主役の面（大）
--sh-focus  0 0 0 3px rgba(27,61,120,.12)       フォーカス・選択状態
```

---

## 代表の登場は2回まで（2026-09-05）

代表・小田はページに **2回だけ** 出す。役割を分ける。

- ヒーローの紺の面 ＝ **誰が担当するか**（顔・根拠3行・氏名）
- 「運営者について」 ＝ **なぜこの価格にしたか**（顔写真・氏名・価格の理由の本文・Edu-NEWSリンク）

旧「代表からひとこと（.say）」は、名乗りが2回になり根拠も重複したため廃止。
価格の理由を語る文（手が回らない／頼むと高い／だから55,000円・保守0円）は
「運営者について」の本文冒頭に置く。**独立した引用セクションを復活させない。**
「運営者について」の本文は、紺の面の根拠（10年・Edu-NEWS）を繰り返さない。
赤ペン（--akapen）はヒーローの1箇所のみ、ここでは使わない。

---

## 料金は縦積み（2026-09-05）

**料金セクションは、上から Lite の帯 → Standard の帯 → 「どちらにも含まれるもの」の面、の
縦積みで組む。左右2列の比較表にしない。** 左右に並べると、目が横に往復して読みづらい。
プランのカードを3枚並べるのも同じ理由で禁止（帯は2枚まで）。

- **帯（`.plan-band`）は全幅・1段。** 左に「プラン名／価格／一言」、右に罫線リスト3行
  （原稿・写真・旧サイトの記事移行）。帯はクリームの面（`.price`）に乗る**白いカード**
- **罫線リストの流儀は月額プラン（`.rate-item`）と同じ。** 行ごとに `border-top`、
  リスト末尾に `border-bottom`。枠付きのセルやピルを作らない
- **Lite の強調は既存の紺（`.suma`）を価格部分にだけ使う。** 帯の地・枠を紺にしない。
  **Standard を「おすすめ」扱いにしない。** 価格は両プランとも同じ大きさ
  （`2.1rem`／390pxで`1.7rem`）
- 「○」「✓」のような記号を使わず、**言葉で書く**
- **Lite と Standard の差は「手間を誰が持つか」（原稿・写真・旧サイトの記事移行）だけ。
  ページ数で差をつけない。** ページ構成は両プランとも標準の8ページ（トップ／コース・料金／
  講師／合格実績／よくある質問／お知らせ・ブログ／アクセス／お問い合わせ）で、
  9ページ目から1ページ8,800円。前置き（`.sec-lede`）には
  「どちらも同じHPができます。違いは、素材を出すか、任せるか」を必ず入れる
- **両プラン共通の中身は帯に書かない。** 帯の下に「どちらにも含まれるもの」の面
  （`.plan-common`・クリーム `--paper` の面1つ）を置き、先頭に標準8ページ、
  続けて2列の罫線リストでまとめる。**≤640px は1列**に落とす
- **確認回数・レクチャー・納期に、勝手な差をつけない。** 文言は FAQ・`makesOffer`・
  `tokushoho.html` の記述と一致させる。片方だけ直さない
- 面の下は **「無料HP診断を申し込む」ボタン1つ**（CTAは1種のまま）と、
  テキストリンク「単価の内訳を見る」だけ
- **390px は帯を縦並びにする。**「名前・価格・一言」の下に罫線リスト3行
- LP構成の「**比較の表は1つ**」は他社との比較の話。他社の実態を断定する表・文を
  料金セクションに持ち込まない

**特典は値引きにしない（2026-09-05）。**

「◯◯円引き」「今なら半額」は作らない。**単価表に正価のある項目を、1つだけ「無料で追加」する**
形にする。二重価格にしないため、**期限を過ぎた申込には実際にその正価を請求する**前提で、
`tokushoho.html` の追加作業料金にも正価として載せる。

- **期限の起点は「無料HP診断の結果をお送りした日」。** 申込日・商談日・月末ではない。
  LP・特商法・診断ページで起点の言い方を揃える（片方だけ直さない）
- 現行の特典は「ドメイン・移転の初期設定 11,000円（税込）を無料」の1つだけ。
  **特典を2つ以上並べない。** 増やすと値引き合戦の見え方になり、正価の説得力が落ちる
- 置き場所は料金セクションの「どちらにも含まれるもの」の面の**下**、CTAの手前。
  **罫線1本の帯（`.plan-perk`）で置く。** 赤ペン・バッジ・ピル型ラベル・カウントダウンを使わない
  （赤ペンはファーストビューの丸つけ1箇所のみ、という既存規約のまま）

**比較表をやめた理由（2026-09-05）。** 2枚のカード＋帯 → 3列の比較表と直したが、
**左と右で並んでいて読みづらい**という指摘に戻った。差は3項目しかないのに、
1行ごとに左右へ目を往復させる形だった。縦積みなら、Lite を読み終えてから
Standard を読める。共通項目は1か所に集約したので、同じ文言が2列に並ぶこともない。

---

## 折り返し（日本語）

```
見出し・一覧タイトル  word-break:auto-phrase; text-wrap:balance
本文・説明文          text-wrap:pretty のみ
```

**本文に `auto-phrase` を掛けない。** 語のまとまりで折るため行が極端に短くなり、
右端が大きく乱れて読みにくくなる（390pxで実測：6行→9行）。
日本語は語中で改行されるのが正常なので、それ自体は不具合ではない。
規約が禁じているのは **1文字だけ次行に残る形**で、これは `pretty` で解消する。

---

## モバイル（〜860px）

- **ナビゲーションを `display:none` にしない。** 2026-08-18 まで記事・一覧ページで
  ナビ4リンクが到達不能になっていた（ハンバーガーも無し）。
  ハンバーガーを足すのではなく、**横スクロールの帯として残す**（JSを増やさない）
- 追従ヘッダーの高さは **画面の15%以内**（390×844で111px＝13%）。
  111pxの内訳は CTA 48px＋ナビ帯 42px＋余白21px で、**これ以上は削れない**。
  ロゴ副題を隠してもCTAが高さを決めるため1pxしか減らず、
  縮めるとタップ領域（現在34px）が推奨下限を大きく割る
- ナビ4リンクは390pxにちょうど収まる（実測 中身390px＝表示幅）。
  **リンクを5つ目に増やすと横スクロールが必要になり、気づかれない**ので増やさない

---

## 一覧ページ

- **セクション一覧（/guide/ /report/ /news/ /case/）にカテゴリのピルを置かない。**
  そのページのカテゴリは自明で、全行に同じ文字が並ぶだけになる
- LPの「お役立ち情報」は複数カテゴリが混ざるので、**あちらには残す**

---

## 法務ページ

`privacy.html` は 2026-08-18 まで**サイト全体と無関係な別テンプレート**だった。
実測：背景 #F8F9FB（青みグレー）、書体 Noto Sans JP、角丸16px×9、
パレットに無いオレンジ #FF6B35、リンクはブラウザ既定の青、ナビ無し、
`site.css` 未読み込み。規約の禁止事項をほぼ全て踏んでいた。

**器は `tokushoho.html` に統一した。** 法務ページを追加・改修するときは、
新しくHTMLを書き起こさず、必ず `tokushoho.html` を複製して中身だけ差し替えること。

---

## 画像の代替テキスト

**記事の画像には必ず alt を書く。** 2026-08-18 まで158点（全体の88%）が空だった。
AIO記事で他塾に対し「画像の代替テキストも0%で、機械には内容が伝わりにくい」と
書いている以上、自社が同じ状態では主張が成り立たない。

教育ニュースの図版は解説スライドなので、**スライド自身の見出しをそのまま alt にする**
（macOS Vision の日本語OCRで抽出。手順は作業ログを参照）。
動画のスクリーンショットはスライドではないので、個別に場面を記述する。
**alt に他社のCTA文言（「すらら公式LINE」等）や動画UIの文字を混入させない。**
alt に `★` を入れない（ビルドの書きかけ判定に引っかかる）。

---

## LP構成（2026-09-04 design-gate で決めたこと）

2026-09-04 のdesign-gateで、LPが全高23,934px・本文24,948字・フォーム到達89%地点まで
膨らんでいた。Codexの採点は「AI生成らしさ8/10・CV設計4/10」。原因は配色ではなく、
論点をすべて独立セクションにした網羅型の構成だった。圧縮後は全高13,894px・8,799字・
フォーム63.6%地点。以後この線を超えない。

- **全高の上限は 1440px で 14,000px、390px で 24,000px。本文は 12,000字以下。**
  超える場合は足す前に削る（記事一覧・比較表の重複・FAQの全文展開が最初に膨らむ）
- **無料HP診断のフォームは全高の 55〜65% 地点に置く。** 説明を全部読ませてから
  行動させる構成にしない。フォームの後ろに置いてよいのは、流れ・運営者・FAQ・記事リンクだけ
- **CTAボタンの文言は「無料HP診断を申し込む」の1種類。** 「診断を受ける／診断してもらう／
  このプランで相談する」を混在させない。副CTAはボタンにせずテキストリンク
- **記事一覧をLPに置かない。** 「塾HPの基礎知識／調査レポート／教育ニュース」の3リンクまで。
  LPは申込の受け皿で、メディアのトップではない
- **比較の表は1つ。** 「終わるまでにいくら払うか」の計算例表だけを置き、他社の実態を
  断定する表・文（「主流」「原則残らない」「A社／B社」）は置かない。他社は計算例として扱う
- **ピル型ラベルを置かない。** 「まずはここから」「追加1」「FREE CHECK」のような
  枠付きの小ラベルは、罫線＋文字の見出しラベル（.sec-label）で代える。英字のあおり
  ラベルは使わない
- **蛍光マーカーは本文中の限定強調のみ。見出しに使わない**（既存規約の再掲。4見出しから外した）
- **順序に意味のない番号を振らない。** お悩みの漢数字、サービスの01〜04は外した。
  残してよいのは「ご相談の流れ」の①②③と「診断8項目」のように順番が情報になるものだけ
- **運営者欄に同形の実績枠を並べない**（「10年+／発信中／実践者／直接担当」の4枠は
  統計バーの変種だった）。本文1〜2文と写真で足りる
- **見出しは「競合が社名を差し替えて使えるか」で判定する。** 使えるなら、55,000円・
  月額保守費0円・自分で更新・マップとAI検索込み、のどれかの事実を入れて書き直す
- 月額プランは4枚のカードではなく、名称／月額／1行説明の罫線リストで置く
- 単価表とFAQの5問目以降は `<details>`（既定は閉）に入れる。DOMには残し、
  FAQPage の構造化データと本文の一致は維持する

---

## 禁止

- 紫グラデーション、意味のないグラデーション
- 意味のないアイコン、装飾目的の図形
- ガラス風の半透明（ヘッダーの blur は既存の1箇所のみ可）
- 同じ形のカードを意味なく3枚横並びにする
- **大きな数字＋小さなラベルを等分割で並べる「統計バー」**。公式が
  "the template answer" として名指しする形。2026-08-18 にファーストビューから撤去した。
  h1で既に述べた事実の再掲でもあり、Signature（赤丸）の一点集中を薄めるため復活させない
- 見出しに蛍光マーカーと赤ペンを同時に使う
- **チェックマーク（✓・○）を箇条書きの行頭に置かない**（2026-09-05）。
  CSSで描いた三角の✓も同じ。DESIGN.mdの「意味のないアイコン」に当たり、
  同時に「○✓を使わず言葉で書く」（料金の規約）を全ページに広げた形。
  箇条書きは**罫線行**（`border-top` を1行ずつ、リスト末尾に `border-bottom`）で組む
- **同じ形のカードを「縦に」3つ積むのも同じ禁止に含める**（2026-09-05）。
  横並びをやめても、枠付きのカードを縦に3つ並べたら反復は残る。枠は2つまで
- **代表写真を四角の枠にはめて置かない。** ファーストビューでは、面からはみ出す
  切り抜きにして、**根拠の文言と組む**（枠に収めた顔写真は「担当者紹介の部品」に
  見えて、誰が担当するのかという主張にならない）
- Lorem ipsum・英語ダミーコピー。**必ず本番の日本語で設計する**
- 「革新的／シームレス／次世代／ビジネスを加速」等の抽象語。
  見出しは「競合が社名だけ差し替えて使えるか」で判定し、使えるなら書き直す

## 2026-09-05 design-gate で決めたこと

**枠線の太さは1本に統一する。** 同じセクションの中で `1.5px solid var(--ink)` と
`1px solid var(--line)` を混ぜない。今回、料金の縦積みが「1.5px濃墨の枠2つ＋1px薄罫の枠1つ」の
3連続になり、横並びの同形カードがそのまま縦に化けていた。
- カードの枠は **`1px solid var(--line)`** を既定とする
- `1.5px solid var(--ink)` を使ってよいのは、**比較の表（`.tbl`）の外枠1箇所だけ**
- 罫線を**書き分けたい**とき（例：料金の「プランの境目」と「項目の境目」）は、
  太さではなく色で分ける。**プランの境目＝`--ink` 1px／項目の境目＝`--line` 1px**

**料金の2プランは1枚の紙の上下2段にする。** 枠を2つ並べない（帯2枚は枠2つと同義だった）。
`.plan-bands`（白いカード1枚）の中に `.plan-band` を2つ置き、境目は `--ink` 1px の横罫1本。
**価格の書式は両プランで完全に同じ。** 片方だけベタ塗り・枠・バッジで囲まない。
「どちらも同じHPができます」と書いた隣で見た目が片方を推していたら、文が嘘になる。

**セクション見出しの型を欠かさない。** すべてのセクションは
`sec-label`（罫線＋短いラベル）＋`sec-title`（明朝の主張）の2段で組む。
ラベルだけ・タイトルだけにしない。**タイトルには「競合が社名を差し替えて使えない文」を置く。**
「料金」「サービス」のような分類語はラベル側に置く。
（2026-09-05：料金だけラベルが無く、見出しが「料金」の2文字だった。
前置きの1行目「どちらも同じHPができます。違いは、素材を出すか、任せるか。」を見出しに上げた。）

**フォーム到達位置の測り方を固定する（Codexの指摘）。**
これまで「フォーム節の先頭」と「最初の入力欄の上端」を混ぜて測っていたため、
改善量が比較できなかった。**以後は次の2つを両方記録する。**

| 測り方 | 1440px | 390px | 規約 |
|---|---|---|---|
| フォーム節（`#diagnosis`）の先頭 | 62.0% | 66.6% | 55〜65% |
| 最初の入力欄（塾名）の上端 | 67.0% | 70.0% | 参考値 |

**55〜65% の判定は「フォーム節の先頭・1440px」で行う**（この数字で過去の記録が
取られているため）。390px は同じ基準で測って**70%を上限**とし、超えたら
フォームより前を削る。**フォームより後ろに動かして帳尻を合わせない**
（後ろに置いてよいのは、流れ・運営者・FAQ・記事リンクだけ、は変更なし）。

**架空の制作サンプルには、架空だと書く。** ブラウザ枠つきの画面を並べると、
断らないかぎり納品実績に見える。`.works-note` の1行
（※3枚とも架空の塾によるデザインサンプルです。実際の納品事例ではありません。）を消さない。

**代表写真は2箇所とも同じ撮影・同じ服装から採る。**
現状はファーストビュー＝白シャツ・ノータイのスタジオ切り抜き（`assets/oda-cutout.png`）、
運営者について＝ネクタイ・腕組み・屋外ボケの別カット（`assets/oda.jpg`）で、
同じ人が2回出る設計なのに連続性がない。腕組み＋屋外は典型的な「社長のストック写真」で、
それ自体がテンプレ感を出す。**次の撮影で運営者側を差し替える。**
差し替えるまでは、この不一致を「直したつもり」にしない。

**折り返しの確認は390pxの実物で行う。** 1〜2文字だけ次行に残る形が出たら `.nb` で語ごと折る。
ただし **`.nb` を本文に掛けすぎない。** 掛けすぎると390pxで行が極端に短くなり、
右端が階段状に乱れて、かえって作りかけに見える（2026-09-05にサービス節で実際に起きた）。
`.nb` は「1〜2文字残り」と「語中割れ」を潰す最小単位にだけ当てる。

## 確認

実際の日本語で `1440px / 1024px / 768px / 390px` を目視。
特に **見出しの折り返しで1文字だけ次行に残っていないか**（`.nb` / `.maru` で語ごと折る）。
ビルドが通っただけでは完成としない。

---

*根拠：Anthropic公式スキル `frontend-design`（claude-plugins-official）。
「ロゴとナビを消しても誰のサイトか分かるか」を改修のたびに1回問うこと。*
